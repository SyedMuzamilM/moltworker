# TOOLS.md

Tool usage rules:
- Use the web tool only when up-to-date facts are required.
- Use file edits for durable outputs and summaries.
- Use shell commands only when needed and avoid destructive actions.
- Do not access secrets unless the task explicitly requires it.
